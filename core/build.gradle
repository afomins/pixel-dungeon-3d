apply plugin: "java"

sourceCompatibility = 1.8
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/", "src-stubs/", "pixel-dungeon/src", "pd3d-lib/src", "pd-classes" ]


eclipse.project {
    name = appName + "-core"
}
